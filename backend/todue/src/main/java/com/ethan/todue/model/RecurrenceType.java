package com.ethan.todue.model;

public enum RecurrenceType {
    DAILY,      // every day
    WEEKLY,     // every week
    BIWEEKLY,   // every other week
    MONTHLY,    // every month
    YEARLY      // every year
}
