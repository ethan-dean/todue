# SECRETS ONLY - Keep this file secure and out of version control
# Non-secret configuration is in application-dev.properties and application-prod.properties

# Database Password
# Note: Database host, port, name, and username are configured in application-{profile}.properties
DB_PASSWORD=your-secure-database-password

# JWT Secret
# Generate a secure secret with: openssl rand -base64 64
# Must be at least 256 bits (32 characters) long
JWT_SECRET=your-super-secret-jwt-key-at-least-256-bits-long

# Email Configuration (Resend)
# Get your API key from https://resend.com/api-keys
RESEND_API_KEY=re_your_resend_api_key_here
EMAIL_FROM=ToDue <noreply@ethandean.dev>
APP_URL=https://todue.ethandean.dev

# Configuration locations:
# - Secrets: .env (this file)
# - Dev config: application-dev.properties (uses td_dev username)
# - Prod config: application-prod.properties (uses td_user username)
# - CORS/WebSocket: application-{profile}.properties
